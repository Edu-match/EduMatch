-- =============================================================================
-- 初回データ移行用の「手動実行スクリプト」用に残していたファイルです。
-- VIEWER / ADMIN をロールだけで自動振り分けする処理はアプリ要件に含めないため、
-- マイグレーションとしては何もしません（必要な場合は SQL Editor で個別に実行してください）。
-- =============================================================================

BEGIN;
COMMIT;
